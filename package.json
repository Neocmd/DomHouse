{
  "name": "domhouse",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "agent": "cd packages/agent-core && npm run dev",
    "ui": "cd packages/ui && npm run dev"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
